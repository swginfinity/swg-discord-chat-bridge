__pycache__/
*.pyc
.venv/
logs/
*.log
node_modules/

# Configs contain bot tokens and passwords — never commit
configs/*.json
!configs/example.json

# local backups (configs/*.bak, pre-change .py snapshots) — never artifacts to track
*.bak
*.bak.*
